@model IEnumerable<WebApplication3.Models.Domain.Publisher>
@{
    ViewData["Title"] = "GetAll";
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <h1>List of Publisher</h1>
            <td><a class="btn btn-dark" href="/Publisher/Add">Add More</a></td>
            <br /><br />
            <table class="table table-striped table-bordered table-dark">
                <tr>
                    <th>Publisher Name</th>
                    <th class="text-center">Action</th>
                    <th class="text-center">Action</th>
                </tr>
                <tbody>
                    @foreach(var item in Model)
                    {
                        <tr>
                            <td>@item.PubliserName</td>
                            <td class="text-center"><a class="btn btn-success" href="/Publisher/Update?id=@item.Id"><i class="bi bi-pen"></i></a></td>
                            <td class="text-center"><a class="btn btn-danger" href="/Publisher/Delete?id=@item.Id" onclick="return window.confirm('Are you Sure Delete ?')"><i class="bi bi-trash"></i></a></td>
                        </tr>
                    }
                </tbody>
            </table>
            
        </div>
    </div>
</div>

